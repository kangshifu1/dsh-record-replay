---
name: dsh-record-replay
description: 使用 DSH 录制回放（Record & Replay）插件的会话回放、回放包导出/导入、会话复刻、录屏与录屏生成技能。当用户提到「录制回放 / record replay / 会话回放 / 复刻会话 / 导出回放包 / 导入回放包 / 录屏 / 录屏生成 skill」时使用本技能。
---

# DSH 录制回放（Record & Replay）

把 DSH 自动录制的会话和浏览器录屏，变成可回放、可分享、可复刻、可沉淀为技能的资产。

## 目标

1. 回放任意已录制会话（时间线：用户消息 / 助手回复含推理 / 工具调用与结果）
2. 导出 / 导入可分享的回放包（dsh-replay-pack JSON，git 友好）
3. 复刻：把录制会话的用户消息按原顺序重新执行到全新会话
4. 录屏并让 agent 逐帧分析，自动生成 SKILL.md 安装为技能

## 何时使用

- 用户要「回放 / 查看某个会话」或「复刻 / 重跑一个会话」
- 用户要「导出 / 分享 / 导入回放包」
- 用户要「录屏生成技能 / 把一段操作变成 SKILL」

## 工作流

### 会话回放 / 导出 / 复刻
1. 打开侧边栏「录制回放」面板，进入「会话」tab
2. 选一个会话，三选一：
   - 回放：以可读时间线查看（搜索过滤）
   - 导出：下载单个 .replay.json 回放包
   - 复刻：把该会话的用户消息逐条发送到全新会话，让 agent 重新执行

### 导入回放包
1. 「回放包」tab 导入 .replay.json
2. 对导入的包：查看 / 删除 / 复刻

### 录屏 → 生成技能
1. 「录屏」tab 点「开始录屏」，选屏幕/窗口（getDisplayMedia）
2. 停止后自动上传 webm 视频 + 每 2s 采样帧
3. 点「生成技能」：插件开一个 agent 会话，让它逐帧调用 describe_image 分析，
   输出 SKILL.md 写入 ~/.dsh/skills/<name>/SKILL.md，即成为可用技能

## 数据位置

- 读：~/.dsh/sessions/**/session.jsonl(.zstd)（DSH 自动录制，插件只读）
- 写：~/.dsh/replay-packs/（导入的回放包）、~/.dsh/recordings/（录屏）、~/.dsh/skills/（生成的技能）

## 注意

- 录屏会捕获屏幕上的一切（含密码输入），生成技能前请脱敏
- 回放包包含完整对话与工具输出，可能含密钥 / 敏感信息，分享前请脱敏
- 会话库 / 录屏均为本机数据，不跨机
